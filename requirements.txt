# Core Dependencies - Güvenli ve Minimal
requests>=2.31.0
beautifulsoup4>=4.12.0
urllib3>=2.0.0
tqdm>=4.65.0
Pillow>=10.0.0
aiohttp>=3.12.0
aiofiles>=23.2.0

# Optional: Selenium for complex sites (if needed)
# selenium>=4.15.0
# webdriver-manager>=4.0.0

# Note: Proxy ve CAPTCHA paketleri güvenlik nedeniyle kaldırıldı
# Güvenli scraping için safe_scraping.py modülü kullanılıyor